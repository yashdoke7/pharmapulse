# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Data — the raw CSV is committed, everything derived is not
data/warehouse/
data/observed/*.parquet
!data/observed/.gitkeep
!data/synthetic/.gitkeep

# Artifacts — benchmarks.json IS committed (the Ops Console reads it)
artifacts/runs/
mlruns/

# Node
node_modules/
web/dist/
web/.vite/

# Env
.env
.env.local
*.log

# frontend build output
web/dist/
web/tsconfig.tsbuildinfo

# deck build deps
deck/node_modules/
deck/package-lock.json
deck/*.pdf
deck/slide-*.jpg

# Office lock files (created when a .pptx/.docx is open)
~$*

# Not ours - a teammate's deck, kept locally only
docs/PharmaPulse_Deck (1).pptx

# Extended dataset drop (11MB, and 2020-2026 is self-labelled synthetic).
# Not committed until we decide how it is allowed to be used - see lane 3.
data/pharma_sales_dataset/

# The generated extension is reproducible from scripts/make_extension.py with
# a fixed seed - the generator is the artefact, not the CSV.
data/synthetic/*.csv
